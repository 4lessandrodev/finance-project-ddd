<!DOCTYPE html>
<html lang="en">
<head>
    <title>Documentação da Api</title>
    <meta name="description" content="Documentação da api de controle financeiro">
    <link rel="icon" type="image/x-icon" href="/favicon-96x96.png">
    <meta name="theme-color" content="#ffffff">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
  </head>
<body style="text-align: center; background-color: #ededed;">
  <h1 style="padding: 2rem;">Documentação</h1>
	<main class="container">
		<nav>

      <div class="row">
        <div class="col-sm-6">
          <div class="card">
            <div class="card-body">
              <h3 class="card-title">Organograma</h3>
              <p class="card-text">Detalhes dos schemas e como eles se relacionam entre si. Também é possível visualizar  as queries disponíveis.</p>
              <a href="/doc" class="btn btn-primary" id="organography-url">Acessar organograma</a>
            </div>
          </div>
        </div>
        <div class="col-sm-6">
          <div class="card">
            <div class="card-body">
              <h3 class="card-title">Documentação</h3>
              <p class="card-text">Mutations, Schemas e Queries. Consultar todas as mutations e queries disponíveis bem como a estrutura de cada entidade.</p>
              <a href="/" class="btn btn-primary">Acessar documentação</a>
            </div>
          </div>
        </div>
      </div>

		</nav>
	</main>
  <script>
    
    var organographyUrl = document.getElementById('organography-url');
    var hostName = location.hostname;
    var url = organographyUrl.href;

    function checkEnvironmentByHostName(hostName) {
      const result = Boolean(hostName === 'localhost') || Boolean(hostName === '127.0.0.1');
      return result;
    }

    const isLocal = checkEnvironmentByHostName(hostName);

    if (isLocal){
      organographyUrl.href = `${url}/local`;
    } else {
      organographyUrl.href = `${url}/remote`;
    }

  </script>
</body>
</html>
